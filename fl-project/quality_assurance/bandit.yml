# bandit.yaml
assert_used:
  skips: ['*_test.py', '*/test_*.py']

exclude_dirs:
  - tests
  - venv
  - .venv

#skips: ['B101'] # Skip assert

profiles:
  fl_security:
    include:
      - B301  # pickle
      - B324  # hashlib
      - B113  # request_without_timeout