version: '3.8'

networks:
  # Connect to the EXISTING network created by infra
  fl_network:
    external: true
    name: fl_shared_network

services:
  server:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile
    container_name: fl_server
    ports:
      - "5000:5000"
    environment:
      - REGISTRATION_TOKEN=NAZIH_IS_THE_GOAT
    volumes:
      - ../../src:/app/src
      - ../../src/utils:/app/src/utils
      - ../../src/data:/app/data
    working_dir: /app/src
    command: python server.py
    networks:
      - fl_network
    logging:
      driver: loki
      options:
        loki-url: "http://localhost:3100/loki/api/v1/push"

  client_1:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile
    container_name: fl_client_1
    environment:
      - CLIENT_ID=client_1
      - SERVER_URL=http://server:5000
      - DATA_FILE=/app/data/client_1_data.json
      - REGISTRATION_TOKEN=NAZIH_IS_THE_GOAT
    volumes:
      - ../../src:/app/src
      - ../../src/utils:/app/src/utils
      - ../../src/data:/app/data
    working_dir: /app/src
    command: python client.py
    networks:
      - fl_network
    depends_on:
      - server
    logging:
      driver: loki
      options:
        loki-url: "http://localhost:3100/loki/api/v1/push"

  malicious_client:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile
    container_name: fl_malicious_client
    environment:
      - CLIENT_ID=malicious_client
      - SERVER_URL=http://server:5000
      - DATA_FILE=/app/data/malicious_client_data.json
      - ATTACK_MODE=${ATTACK_MODE:-NONE}
      - REGISTRATION_TOKEN=NAZIH_IS_THE_GOAT
    volumes:
      - ../../src:/app/src
      - ../../src/utils:/app/src/utils
      - ../../src/data:/app/data
    working_dir: /app/src
    command: python malicious_client.py
    networks:
      - fl_network
    depends_on:
      - server
    logging:
      driver: loki
      options:
        loki-url: "http://localhost:3100/loki/api/v1/push"